name: fit_pro
description: FIT PRO - All-in-one online fitness tools app
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^3.2.0
  connectivity_plus: ^4.0.1
  shared_preferences: ^2.1.1
  permission_handler: ^11.3.0
  url_launcher: ^6.2.12
  package_info_plus: ^2.0.4   # Older version compatible with older Flutter SDK

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/instagram.png
